# Square Developer Dashboard → your app → Sandbox (or Production) tab
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=sandbox

# Resend (confirmation emails) — resend.com → API Keys
RESEND_API_KEY=
# Optional override; defaults to "The Unraveling <admin@fixatephx.com>"
RESEND_FROM=
